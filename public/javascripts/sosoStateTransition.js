function sosoStateTransition(iStartState, iEndState){

	this.startState = iStartState;
	this.endState = iEndState;

	// console.log("sosoStateTransition- new transition. startState : "+ this.startState + " | endState : " + this.endState);
}
sosoStateTransition.prototype = {
	
}